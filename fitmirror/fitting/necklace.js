/**
 * 목걸이 피팅 (Phase 2 구현 예정)
 *
 * Phase 1: MediaPipe Pose 없이 단순 이미지 오버레이로 목 위치에 배치
 * Phase 2: Pose LEFT_SHOULDER + RIGHT_SHOULDER 중점 기반 베지어 커브 워프
 *
 * @param {CanvasRenderingContext2D} _ctx
 * @param {Array|null} _poseLandmarks
 * @param {HTMLImageElement} _necklaceImage
 * @param {number} _canvasW
 * @param {number} _canvasH
 */
// eslint-disable-next-line no-unused-vars
export function drawNecklace(_ctx, _poseLandmarks, _necklaceImage, _canvasW, _canvasH) {
  // Phase 2에서 구현
}
